Redraw every text/symbol inside this grid as if hand-written by a machinist on a
shop-floor engineering drawing, using a black ballpoint pen or pencil.

CRITICAL constraints:
- This image has a grid of 12 separate cells, each bordered by a black rectangle.
  Preserve the exact grid layout, cell positions, and cell border lines. Do not
  merge, resize, move, or reorder cells.
- Inside each cell, preserve the EXACT characters, digits, and symbols shown —
  do not add, remove, or change any character. Content must stay identical to
  the input, cell by cell.
- Convert the clean printed/CAD-font strokes into natural handwriting: uneven
  stroke width, slight waviness, natural pressure variation, imperfect corners.
  Use the SAME handwriting style/pen across all cells (as if one person wrote
  the whole sheet).
- Functional field-annotation style, not decorative or cursive calligraphy.
- Background stays plain white outside the ink strokes. Single ink color
  (black or blue), no gradients, no shadows, no paper texture unless requested.
- Do not let content from one cell bleed into a neighboring cell.
